__pycache__/
*.pyc
.env
data/*
!data/manual_events.json
!data/major_tulsa_events.json
logo/tulsa_skyline_source.jpg
data/fb_auto_profile/
data/fb_group_auth.json
data/GROUP_AUTH_DEAD.flag

# Graphic approval registry must be version-controlled (William sign-offs)
!data/approved_assets.json

# Venue corrections + recurring-event verification ledger are operator
# source-of-truth (monthly venue overrides, dead/paused decisions) - track + sync
!data/venue_overrides.json
!data/recurring_confirmations.json

# Event-of-the-Week pins are a curator decision (a human verified this event is
# the week's hero), so they must survive to every machine that generates a week.
!data/manual_eotw.json

# Local debug/backup debris - keep on disk, never commit (added 2026-07-01)
*.bak
*.bak-*
tools/diag_*.py
drafts/
